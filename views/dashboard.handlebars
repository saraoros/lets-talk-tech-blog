<section class="new-post-section">
    <button id="create-new-post" type="submit" class="btn">Create a New Post</button>
</section>


{{#if posts.length}}
<section>
    <h2> Dashboard</h2>
    <ul class="post-list">
        {{#each posts as |post|}}
        <li>
            {{> post-info post}}
            <a href="/dashboard/edit/{{post.id}}" class="edit-link">Edit Post</a>
        </li>
        {{/each}}
    </ul>
</section>
{{/if}}

<script src="/javascript/create-post.js"></script>
